# List discovery resources

Use this skill to find machine-readable resources published by Blog.js.gripe.

## Tool

Call the WebMCP `list_discovery_resources` tool on `https://blog.js.gripe/`.

## Output

The tool returns URLs for the API catalog, agent skills index, MCP server card, OAuth protected resource metadata, `auth.md`, `llms.txt`, and `llms-full.txt`.
